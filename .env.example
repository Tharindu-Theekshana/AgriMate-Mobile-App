# Mobile app config. Copy to .env (Expo auto-loads EXPO_PUBLIC_* vars).
# Point this at your backend. For a physical phone, use your computer's LAN IP.
# Defaults if unset: Android emulator → http://10.0.2.2:8080, else http://localhost:8080
EXPO_PUBLIC_API_URL=http://192.168.1.100:8080
